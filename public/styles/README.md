# Spark AI Static Styles

This folder is reserved for public, static style assets that need to be served directly from `/styles/*`.

Application CSS, Tailwind layers, and shadcn variables live in `app/globals.css`.
