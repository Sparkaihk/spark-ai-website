# AGENTS.md

本文件定义 Spark AI Website 项目的固定自动化协作流程。每次执行任务前，先判断任务归属；执行完成后，必须按统一格式输出交付结果。

## Project Context

- 项目类型：Next.js 网站项目
- 本地开发：`npm run dev`
- 构建验收：`npm run build`
- 生产预览：`npm run start`
- 主要代码目录：
  - `app/`
  - `components/`
  - `lib/`
  - `public/`
  - `docs/`

## Mandatory Task Classification

每次开始执行前，必须先判断任务属于以下三类之一，并在回复或工作记录中明确说明：

1. Website Automation
2. Investor/Government Materials Automation
3. Business Calculator Automation

如果任务同时跨多类，选择主流程，并说明涉及的辅助流程。

## 1. Website Automation

适用范围：

- 官网页面、落地页、导航、页脚、布局、响应式样式
- 产品页、解决方案页、行业页、联系页、关于页
- 站点组件、设计系统、视觉资产、动画与交互
- SEO metadata、站点文案、本地化文本

执行流程：

1. 识别受影响页面、组件和内容来源。
2. 优先复用现有组件、设计 token、页面结构和视觉语言。
3. 保持移动端和桌面端布局稳定，避免文字溢出、元素重叠和无意义装饰。
4. 如涉及视觉资产，优先使用 `public/` 中已有资源；新增资源需命名清晰并放入合适目录。
5. 完成后至少运行 `npm run build`；如涉及交互或布局，启动 `npm run dev` 做本地预览。

验收重点：

- 页面可正常构建
- 主要视口下布局不破碎
- 导航路径、链接、按钮和核心 CTA 可用
- 文案、品牌调性和视觉风格一致

## 2. Investor/Government Materials Automation

适用范围：

- 投资人首页、投资人 deck、政府申报材料、招商材料
- 商业计划、项目介绍、路线图、市场和财务叙事
- 面向资本、政府、园区、战略合作伙伴的正式材料
- `app/investors/`、`docs/` 以及相关内容数据

执行流程：

1. 先判断材料受众：投资人、政府、园区、企业客户或混合受众。
2. 明确叙事主线：问题、方案、技术壁垒、商业模式、落地进度、资金用途或政策价值。
3. 保持数字、阶段、时间线、产品命名和公司定位前后一致。
4. 对外材料避免夸张承诺；关键表述应可被审阅、复核或进一步替换成正式数据源。
5. 如涉及页面展示，运行 `npm run build`；如涉及文档或内容结构，检查相关页面和文档引用是否同步。

验收重点：

- 叙事完整、可信、面向目标受众
- 产品、市场、财务和路线图表述一致
- 投资或政府语境下没有明显不严谨承诺
- 相关页面或文档可正常打开、构建或预览

## 3. Business Calculator Automation

适用范围：

- ROI、TCO、收入预测、成本测算、容量测算、定价模拟
- 业务计算器页面、表单、公式、结果展示
- 财务模型、商业假设、单位换算、敏感性分析

执行流程：

1. 明确输入项、默认值、单位、边界条件和输出指标。
2. 将核心计算逻辑与 UI 展示分离；优先放在 `lib/` 或明确的计算模块中。
3. 对公式使用可读命名，避免硬编码在组件 JSX 中。
4. 为异常输入、空值、极端值和单位转换设置合理处理。
5. 完成后运行 `npm run build`；如新增可测试函数，补充或执行相应测试命令。当前项目未配置专用测试脚本时，至少进行手动案例验算。

验收重点：

- 公式和单位清晰
- 默认案例、边界案例和异常输入结果合理
- UI 不因长数字、空状态或错误状态破坏布局
- 计算假设在页面或相邻文案中可理解

## Required Completion Report

每次执行任务后，必须输出以下四项：

```markdown
**流程归类**
Website Automation / Investor/Government Materials Automation / Business Calculator Automation

**修改了哪些文件**
- `path/to/file`

**如何本地测试**
- `npm run build`
- 如需预览：`npm run dev`

**是否通过验收**
通过 / 未通过 / 部分通过
说明关键验证结果；如未运行测试，必须说明原因。

**下一步建议**
- 给出最直接、最有价值的后续动作。
```

## Working Rules

- 不覆盖用户已有改动，除非用户明确要求。
- 编辑前先理解现有结构，优先沿用项目模式。
- 保持改动聚焦，避免无关重构。
- 生成或修改对外材料时，优先保证可信度、一致性和可审阅性。
- 涉及网站体验时，关注真实可用的页面结果，而不是只完成代码形态。
- 涉及计算器时，优先保证公式正确、单位清楚、边界稳定。
